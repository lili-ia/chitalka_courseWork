<UserControl x:Class="chitalka_courseWork.MVVM.View.LibraryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:chitalka_courseWork.MVVM.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel>

        <StackPanel.Resources>
            <Style TargetType="{x:Type Border}">
                <Setter Property="Margin" Value="25,0,0,0"/>
            </Style>
        </StackPanel.Resources>
        
        <TextBlock Text="Результати"
                   Foreground="White"
                   FontSize="28"
                   HorizontalAlignment="Left"
                   Margin="0, 0, 0, 20"/>

        <StackPanel Orientation="Horizontal" Name="bookStackPanel" >
            
            <Border Width="150"
                    Height="200">

                <Border.Background>
                    <LinearGradientBrush StartPoint="0,0"
                                         EndPoint="1,2">
                        <GradientStop Color="#B6BBC4" Offset="0.0"/>
                        <GradientStop Color="#31304D" Offset="1"/>
                        
                    </LinearGradientBrush>     
                </Border.Background>

                <Border.Clip>
                    <RectangleGeometry RadiusX="10"
                                       RadiusY="10"
                                       Rect="0,0,150,200"/>
                </Border.Clip>

            </Border>

            <Border Width="150"
                    Height="200">

                <Border.Background>
                    <LinearGradientBrush StartPoint="0,0"
                             EndPoint="1,2">
                        <GradientStop Color="#B6BBC4" Offset="0.0"/>
                        <GradientStop Color="#31304D" Offset="1"/>

                    </LinearGradientBrush>
                </Border.Background>

                <Border.Clip>
                    <RectangleGeometry RadiusX="10"
                           RadiusY="10"
                           Rect="0,0,150,200"/>
                </Border.Clip>

            </Border>

            <Border Width="150"
                    Height="200">

                <Border.Background>
                    <LinearGradientBrush StartPoint="0,0"
                             EndPoint="1,2">
                        <GradientStop Color="#B6BBC4" Offset="0.0"/>
                        <GradientStop Color="#31304D" Offset="1"/>

                    </LinearGradientBrush>
                </Border.Background>

                <Border.Clip>
                    <RectangleGeometry RadiusX="10"
                           RadiusY="10"
                           Rect="0,0,150,200"/>
                </Border.Clip>

            </Border>

            <Border Width="150"
                    Height="200">

                <Border.Background>
                    <LinearGradientBrush StartPoint="0,0"
                             EndPoint="1,2">
                        <GradientStop Color="#B6BBC4" Offset="0.0"/>
                        <GradientStop Color="#31304D" Offset="1"/>

                    </LinearGradientBrush>
                </Border.Background>

                <Border.Clip>
                    <RectangleGeometry RadiusX="10"
                           RadiusY="10"
                           Rect="0,0,150,200"/>
                </Border.Clip>

            </Border>
            

        </StackPanel>
   
    </StackPanel>
</UserControl>
